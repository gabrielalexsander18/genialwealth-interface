import styled from "styled-components";
import { theme } from "../../styles/theme";

export const Titulo = styled.p `
    color: ${theme.clearMode.Green};
    font-size: 300px;
    font-weight: bold;
`